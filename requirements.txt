# Core orchestration
langgraph>=0.2.40
langchain-core>=0.3.0
pydantic>=2.5
python-dotenv>=1.0

# LLM providers (optional at runtime — system falls back to deterministic MOCK mode without a key)
groq>=0.11             # Groq (free tier, fast open models)
google-genai>=1.0      # Gemini (free tier)
anthropic>=0.40        # Claude (alternative)

# Web UI (Google-Meet-style room)
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9   # audio upload for STT

# Testing
pytest>=8.0
httpx>=0.27               # FastAPI TestClient

# --- Speech-to-text (server-side; the browser handles text-to-speech) ---
faster-whisper>=1.0
